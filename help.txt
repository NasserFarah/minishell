Category 6: Arguably real, but outside your builtin/feature list

  - . (dot/source command): not in your required builtins list (echo, cd, pwd, export, unset, env, exit) —
    bash has it as a builtin, you don't, so "command not found" instead of bash's dedicated dot-usage error.
    Not required.
  - ABC=hola (bare assignment, no command): the subject only requires export for setting variables; a bare
    VAR=value with no leading command is a separate POSIX shell feature it never mentions. Currently you
    treat it as "run ABC=hola as a command" → not found. This one has a /!\ A gerer comme une erreur (⚠
    handle as an error) note in the sheet, meaning even bash-with-quirks acknowledges it's a corner case —
    but note bash's actual exit code is 0, silent, not an error. If you have spare time, this is the one
    item in this whole batch I'd actually consider implementing, since evaluators sometimes test it even
    though it's not in the mandatory list.

  Bottom line: none of the 41 cases you pasted are mandatory-part bugs. They're all bonus features you
  haven't built (&&/||/(), wildcards), explicitly-excused behavior (backslash), or extensions the subject
  never asked for ($'...', positional/dot-command). Your 67%/83% pass rates on 01_syntax/02_echo are dragged
  down almost entirely by this test bank including bonus/reference-shell cases beyond the subject — the
  mandatory-relevant subset is passing.

  I'll follow up once the full leak-checked run (646 cases) finishes in the background with the actual
  leak/valgrind tally.

✻ Cogitated for 3m 47s · done 10:41 PM

● Background command "timeout 590 tests/run_tests.sh >
/tmp/claude-101673/-home-abdunass-42-minishell/8ebc6015-ee41-4a2f-8594-c7586091280f/scratchpad/full_run.txt
2>&1; echo "EXIT=$?"; tail -30
/tmp/claude-101673/-home-abdunass-42-minishell/8ebc6015-ee41-4a2f-8594-c7586091280f/scratchpad/full_run.txt"
completed (exit code 0)

● The full leak-checked run finished. Let me pull the results.

  Read 1 file (ctrl+o to expand)

● The full leak-checked run of all 646 cases just completed.

  Leak/valgrind-error failures: 0 out of 646. Every single case — including all the pipe, redirection, and
  heredoc paths that fork children — ran clean under valgrind --leak-check=full. That confirms what I saw
  earlier on the smaller samples: memory management is solid across the board, not just in the cases I
  hand-picked.

  Overall: 530/646 passed (82%), up slightly from my earlier partial-category run (44/65 → 44/65 unchanged
  on syntax, but 10_batards improved from 14/14 to 13/14 pass and 09_binaries stayed 3/4 — both effectively
  noise-level, single-case swings). The category breakdown matches what I already dug into:

  ┌─────────────────────┬─────────┬─────────────────────────────────────────────────────────────────────┐
  │      Category       │  Pass   │                                 Why                                 │
  │                     │  rate   │                                                                     │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 05_exit             │ 100%    │ fully covered                                                       │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 08_redirections     │ 98%     │ fully covered, 2 stragglers likely 2>&1-style fd-dup (not required) │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 07_pipes            │ 96%     │ solid                                                               │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 10_batards          │ 92%     │ solid                                                               │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 06_heredoc          │ 78%     │ <<< here-strings + $'...' (not required)                            │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 03_cd_pwd           │ 84%     │ mostly cd -P/edge symlink cases, not required                       │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 02_echo             │ 83%     │ wildcards + backslash + $'...' (all explicitly out of scope, as I   │
  │                     │         │ broke down last message)                                            │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 09_binaries         │ 75%     │ 1 case, small sample                                                │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 01_syntax           │ 67%     │ &&/||/()/wildcards (bonus), backslash (out of scope)                │
  ├─────────────────────┼─────────┼─────────────────────────────────────────────────────────────────────┤
  │ 04_env_export_unset │ 64%     │ the _ special-var issue I flagged earlier, dominating this category │
  └─────────────────────┴─────────┴─────────────────────────────────────────────────────────────────────┘

  Nothing new here changes my earlier assessment — it confirms it with the full run instead of samples. The
  two things still worth doing before submission are the same two I called out as concrete, verified bugs:
  $? not becoming 130 after an idle Ctrl-C, and _ leaking into export/env (which explains most of that 36%
  gap in category 04). Everything else failing is bonus/out-of-spec syntax the subject itself excuses.
